%% Configuração das Citações

%% Estilo
%\usepackage[num]{abntex2cite}			% Citações numéricas
\usepackage[alf, 
 abnt-emphasize = bf, 
abnt-etal-list = 3,
abnt-etal-text = it, 
abnt-and-type = &, 
abnt-last-names = abnt, 
abnt-repeated-author-omit = no  '____.']{abntex2cite}			% Citações "AUTOR, ano"

% Patch para NBR 10520:2023 (Citações em minúsculas dentro dos parênteses)
\makeatletter
\DeclareRobustCommand{\cite}[2][]{%
  \leavevmode
  \let\@citeprevious\@empty%
  \@cite{%
    \@for\@citekey:=#2\do%
      {%
       \edef\@citekey{\expandafter\@firstofone\@citekey\@empty}%
       \expandafter\let\expandafter\@citename%
           \csname b@\@citekey++EXPL\endcsname%
       \expandafter\let\expandafter\@citeyear%
           \csname b@\@citekey++YEAR\endcsname%
       \ifx\@citeprevious\@empty%
       \else%
         \ifx\@citename\@citeprevious%
            \ABCIcitecommadefault%
         \else%
            \ABCIcitecolondefault%
         \fi%
       \fi%
       \if@filesw\immediate\write\@auxout{\string\citation{\@citekey}}\fi%
       \@ifundefined{b@\@citekey++EXPL}%
         {%
          {\bfseries??}%
          \G@refundefinedtrue%
          \@latex@warning{Citation `\@citekey' on page \thepage \space undefined}%
         }%
         {%
           \ifx\@citename\@citeprevious%
             {\yearstyle\@citeyear\@}\@%
           \else%
             {\authorstyle\@citename}, %
             {\yearstyle\@citeyear\@}%
           \fi%
           \let\@citeprevious\@citename%
         }%
         \ifthenelse{\boolean{ABNTbackref}}{\Hy@backout{#2}}{}%
      }%
  }{#1}%
}
\makeatother

% Definição do negrito

%% Colocar entre parênteses ou colchetes?
%% Padrão: Parênteses
%% * Obs: Fica mais agradável usar colchetes quando se usa citações numéricas
%\citebrackets[]							% Descomente essa linha e o documento usará colchetes


%% Configura o "Citado nas Páginas ..." nas referências
%% Não mexa nesse:
\renewcommand{\backref}{}

%% Esse é o texto do "Citado nas páginas ..."
\renewcommand*{\backrefalt}[4]{}